# This recipe reads credentials from the nearest .env, walking up from this
# folder -- by default that's agents-cookbook/.env at the cookbook root, so
# one paste there covers every recipe. Copy this file to a local .env in
# THIS folder only if you want to override the shared config for this
# recipe specifically.

ANTHROPIC_API_KEY=
# Only the DPO pair-construction demo makes real API calls (3 total) --
# the GRPO, DAPO, and credit-assignment demos are pure, deterministic math
# on toy reward groups, per this project's own standing rule that training
# content stays conceptual plus a CPU-only toy, no GPU run.
ANTHROPIC_MODEL=claude-haiku-4-5
